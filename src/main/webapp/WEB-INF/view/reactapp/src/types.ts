export interface Record {
  id: string;
  from: Date;
  to: Date;
  employer: string;
  customer: string;
  projectName: string;
  projectDescription: string;
  technologies: string;
  role: string;
  // other properties
}