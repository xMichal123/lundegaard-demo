import ResponsiveMenuManager from './core/ResponsiveMenuManager';

class Navigator {
    constructor(menu: ResponsiveMenuManager) {
        menu.addMenuItem('🏡 Home', () => {

        });
        menu.addMenuItem('What We Do', () => {

        });
        menu.addMenuItem('About Us', () => {

        });
        menu.addMenuItem('Contact Us', () => {

        });
    }
}

export default Navigator;